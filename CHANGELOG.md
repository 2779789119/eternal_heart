# 永恒之心 (Eternal Heart) 更新日志

## 1.1.9 (2026-07-03)

### 改进
- **配置文件数值范围大幅拓宽**：所有 `Int` 上限改为 `Integer.MAX_VALUE` (2147483647)，所有 `Double` 上限改为 `Double.MAX_VALUE`，不再限制玩家自定义数值上限
  - 属性：攻击伤害/速度/生命/护甲/韧性/击退抗性/移动速度/幸运
  - 防御：伤害减免/摔落/反伤/爆炸减免/受伤上限
  - 回复、战斗强化、采集强化、生存保障
  - 环境适应、光环范围、永怒系统、经验倍率
  - 探索便利、战斗生存增强、机动便利
- 部分下限也同步放宽：不死冷却 0（可禁用）、自动钓鱼间隔 1 tick、冲刺力度 0（关闭）

## 1.1.8 (2026-07-03)

### 修复
- **水上行走/岩浆行走最终修复**：从 `setPosRaw`（不更新碰撞箱，被流体物理覆盖）改为 `setPos`（更新碰撞箱）
  + 深度下陷（>0.3 格，如从高处跌落）时自动用 `teleportTo` 完全重置状态
  + 轻微下沉（>0.005 格）用 `setPos` 平滑修正，无画面抖动
  + 归零垂直动量 + `setOnGround(true)` 在 `travel()` 之前拦截流体下沉

- **爬墙修复**：不再依赖服务端不可靠的 `horizontalCollision` 和 `player.zza`
  + 改为方块检测：检查玩家面前 0.4 格处脚部+胸部两层是否有实心方块
  + 移动方向判断：优先用 `player.zza` 输入，其次用水平速度向量点积

- **负面效果抽搐修复**：彻底解决"清除→重新施加→视角闪烁"问题
  + 新增 `MobEffectEvent.Applicable` 事件处理器 — 在效果被添加**之前**直接 `DENY` 拒绝
  + 有害效果（`MobEffectCategory.HARMFUL`）和属性削减效果永远不会进入玩家活跃列表
  + 保留原有 `clearNegativeEffects` 作为兜底清理

- **自动门关门修复**：不再使用 `distSqr` + 缓存的 `BlockState`
  + 改用真实欧几里得距离（≤1.5 格开门，>2.5 格关门，中间缓冲区防抖动）
  + 关门前重新 `getBlockState` 读取最新状态（修复手动关门后无法检测的 bug）
  + 检测频率提升至每 5 tick（原 10 tick），响应更快

- **无限水/岩浆桶重写**：彻底简化 — 只处理手中的空桶，不扫背包
  + 手中空桶 → 删除空桶 → 立即给予满桶（水源优先于熔岩，搜索 3 格）
  + 每 tick 运行，消耗后 0.05 秒内自动补满

### 修复
- **自动补货彻底修复**：
  - 新增 `getCount() <= 0` 双重守卫防止空位填充
  - 不可堆叠物品（`maxStackSize <= 1`）不再触发补货
  - 搜索范围限定主物品栏（索引 9-35），不碰盔甲/副手
  - 物品匹配改为 `getItem() ==` 宽松匹配（忽略 NBT）
  - 只搬运数量更多的堆（`invStack.getCount() > hotbarStack.getCount()`）

## 1.1.6 (2026-07-03)

### 改进
- **水上行走/岩浆行走重写**：从 `teleportTo` 改为 `setPosRaw` + `setPos` 平滑调整位置，不再有画面抖动
  - 潜行时正常下沉穿过流体
  - 已潜入水/岩浆内部时不触发（防止水下被推回表面）
  - 流体表面高度精确计算（`getOwnHeight()`）
  - 参考 MineFargo 的 `FluidCollisionEvent` 设计模式

## 1.1.5 (2026-07-03)

### 修复
- **末影箱便携修复**：恢复 `RightClickBlock` + 空手检测，并跳过有 GUI 的方块（箱子/熔炉等）。现在潜行空手右键普通方块也能打开末影箱，且不会干扰箱子交互。
- **自动补货修复**：不再填补空快捷栏格子，只补充已有但快用完的物品。

### 新增
- **自动关门**：离开门 2 格以上自动关门

## 1.1.4 (2026-07-03)

### 修复
- **末影箱便携修复**：移除 `RightClickBlock` 处理器，改为仅 `RightClickEmpty`（右键空气）触发，并增加空手检测。不再在潜行放置方块时误开末影箱。

## 1.1.3 (2026-07-03)

### 新增 14 个特性
- **自动火把**：光照低于配置值时自动从背包放置火把
- **自动门**：靠近门自动打开
- **洞穴高亮**：海平面以下时周围实体和掉落物自动发光
- **自动钓鱼**：手持鱼竿站在水边自动钓鱼（含宝藏掉落）
- **宝箱磁铁**：自动收集附近箱子中的物品到背包
- **击退免疫**：独立开关控制
- **永久幸运**：永久幸运药水效果
- **低血自动喝药**：生命低于阈值自动使用背包治疗药水
- **受伤闪电反击**：受伤时概率对攻击者召唤闪电
- **击杀连锁**：击杀怪物时伤害周围同种生物
- **冲刺**：双击前进键(W)短距离冲刺
- **自动补货**：快捷栏空了自动从背包补充同种物品
- **无限水/岩浆桶**：空桶自动从附近流体源重新填满

## 1.1.2 (2026-07-03)

### 修复
- **末影箱远程修复**：将 `RightClickEmpty`/`RightClickBlock` 处理器优先级设为 `LOWEST`，确保在神秘遗物（七咒之戒/终界之戒）之后运行，避免事件被截断

## 1.1.1 (2026-07-03)

### 修复
- **飞行修复**：`mayfly` 现在在客户端和服务端同时设置，双跳飞行正常工作
- **虚空救援修复**：阈值从固定 Y=-32 改为基于世界实际最低高度（`getMinBuildHeight() - 4`），主世界仅基岩层以下触发
- **水上行走/岩浆行走修复**：从简单叠加速度改为检测玩家位置+强力推送至表面+归零垂直动量，不再沉入流体
- **描述文本动态化**：物品提示现在读取实时配置值，修改配置后工具提示自动更新

## 1.1.0 (2026-07-03)

### 修复
- 修复 `Player.getRespawnPosition()` 编译错误（1.20.1 需 `ServerPlayer`）
- 修复 `Entity.maxUpStep` 不可见错误（改用方块检测模拟自动上坡）

## 1.0.0 (2026-07-02)

### 初始版本
- 永恒之心 Curios 饰品
- 全套泰拉瑞亚 Fargo's Soul of Eternity 还原功能
- 属性、防御、生命回复、被动效果、机动强化、战斗强化
- 采集强化、生存保障、环境适应、光环系统
- 永怒叠层系统
