# 永恒之心 (Eternal Heart) 更新日志

## 1.19.4 (2026-09-22)

### 新增 — 时停发动音效
- 发动时停成功时，在发动者位置播放专属音效（`eternal_heart:shi_ting`），
  附近玩家均可听到；音效事件注册于 `sounds.json`，并带字幕提示（中英双语）。

## 1.19.3 (2026-09-22)

### 新增 — 启示录兼容：自动检测佩戴饰品
- 新增配置项 **自动检测佩戴饰品**（`revelationfixAutoDetect`，默认关闭）：
  开启后每 5 秒自动扫描所有在线玩家佩戴中的饰品（Curios 饰品栏），
  把佩戴中的物品 ID 一并写入 RevelationFix 的「天启」豁免名单——
  玩家戴上新饰品即自动保护，饰品被卸下且无人佩戴时自动移除。
- 佩戴 / 卸下饰品、玩家上线 / 下线都会立即触发重扫，不必等例行周期。
- 该功能依赖「启示录兼容」总开关，三者（永恒之心 + 自定义列表 + 自动检测）合并生效。
- 新增命令 `/eternalheart compat revelationfix auto [on|off]`，
  可即时开关自动检测并立即重扫；不带参数时查询当前状态。

## 1.19.2 (2026-09-22)

### 新增 — 自定义饰品防收
- 在「模组集成 → 启示录兼容」下新增配置项 **自定义饰品防收**（`revelationfixExtraItems`）：
  除永恒之心本身外，可额外指定一批物品 ID，一并写入 RevelationFix 的「天启」豁免名单，
  使这些饰品在下界亚波伦「末日终结」阶段同样不被夺取。
- 关闭「启示录兼容」开关时，会连同这些自定义条目一起移除（幂等，不触碰其它条目）。

## 1.19.1 (2026-09-22)

### 变更 — 自定义配方保存后立即生效
- **面板保存即生效**：改完配方点「保存并关闭」后自动重新注入配方表，
  不再需要手动执行 `/eternalheart recipe reload`。
  （该命令保留，用于直接改 toml 文件后手动触发；`/reload` 与重启仍可用）
- **修复重复注入残留**：之前从「现有配方表」出发追加配方，连续 reload 或删改配置后，
  旧的自定义配方会残留在配方表里（删掉一条配方后它依然存在）。
  现在注入前先按 ID 前缀（`eternal_heart_custom_`）剔除本模组上次注入的配方，
  删除 / 修改配置都能正确生效。

## 1.19.0 (2026-09-22)

### 新增 — 配置面板悬浮提示
- 鼠标悬停在任意配置行上即可看到该功能的说明，不必再翻 toml：
  - **第一行**：配置项名称（金色）
  - **中间**：说明文字 —— 优先取语言键 `config.eternal_heart.desc.<字段>`（便于本地化），
    没有时自动回退到配置 schema 的注释（即 toml 里那段说明），因此 140 多项立刻都有可读提示
  - **末行**：**当前值 · 默认值**（深灰）——数值按显示单位呈现（秒 / 百分比等自动换算），
    布尔显示开/关，列表显示项数，飞行模式显示本地化名称
- 提示绘制在面板最上层，随鼠标位置与滚动、分类切换实时更新

## 1.18.0 (2026-09-22)

### 新增 — 自定义配方的图形化编辑器
- 面板「自定义配方」行点「编辑」现在直接进入**图形化编辑器**（`RecipeEditorScreen`）：
  - **3×3 网格**：左键点格子选物品，右键清空；格子悬停显示物品名
  - **产出槽 + 数量**（1~64，`-` / `+` 调整）
  - **模式切换**：有序合成 / 无序合成 / 熔炼
    - 有序：网格位置即配方形状（自动裁掉四周空行空列，画在中间也能得到正确形状）
    - 无序：只看放了什么，相同物品自动合并计数
    - 熔炼：只用左上角一格作为材料
  - **已配置配方列表**：点击可**载回编辑器修改**（含标签 `#tag` 的配方无法还原成具体物品时会提示），
    支持「更新选中」与「删除选中」
  - **文本模式**按钮：仍可切到简化语法文本编辑（复杂配方、标签配方用）
- 新增**物品选择器**（`ItemPickerScreen`）：列出全部已注册物品（含其它模组），
  显示图标与本地化名称，支持按名称或 ID 搜索，悬停显示完整 ID
- 图形化编辑的产物仍是简化语法文本，与手写、配置文件完全一致 —— 只是把这段文本"点"出来

## 1.17.0 (2026-09-22)

### 变更 — 自定义配方改用简化语法（一行一条）
- 新增 `RecipeText` 解析层：**简化语法**自动翻译成原版配方 JSON，同时**原版 JSON 仍可直接粘贴**
  （向后兼容，复杂配方照旧手写）：
  - 无序合成：`shapeless: diamond_block = 9x diamond`
  - 有序合成：`shaped: diamond_pickaxe = diamond diamond diamond / _ stick _ / _ stick _`
    （`/` 分行、空格分格、`_` 表示空位）
  - 熔炼之类：`smelting: glass <- sand`（`blasting:` / `smoking:` 同理）
  - 数量：`9x diamond` 与 `diamond x9` 两种写法都支持
  - 物品 ID 可省略 `minecraft:` 前缀；`#` 开头表示标签（如 `#minecraft:planks`）
- 配置项注释与面板编辑器提示都给出了一行示例，不用再对着 JSON 拼括号
- 无法识别的条目仍只记日志并跳过，且日志会区分「无法识别」与「解析失败」两种原因

## 1.16.3 (2026-09-22)

### 修复 — 时停没有暂停弹射物
- **原因**：弹射物（箭、火球、雪球、三叉戟、烟花……）各自重写了 `tick()`，
  并且**不调用** `Entity#tick` —— 之前只拦基类方法，对它们完全无效。
- **修复**：新增 `LevelFreezeMixin`，拦在 `Level#guardEntityTick` ——
  这是世界 tick 每个实体的**必经入口**（服务端 `ServerLevel.tickNonPassenger` 与
  客户端 `ClientLevel.tickEntities` 都经由它），一次覆盖生物 / 弹射物 / 掉落物 /
  经验球 / 矿车 / TNT 等全部实体。取消整个 consumer 还使 `setOldPosAndRot` 不再执行，
  位置与朝向都不推进。
- **客户端同步**：实体 tick 在客户端也会跑（插值、动画、弹射物本地飞行），
  只冻结服务端的话箭在本地仍会继续飞。新增 `TimeStopNetwork.Sync`（S→C）把状态同步给客户端，
  客户端据此一并冻结；两张状态表彼此独立，避免单人游戏（同一 JVM）下客户端递减吃掉服务端状态。
  状态每 10 tick 重播一次，中途进入维度的玩家也能收到。
- 原有 `Entity#tick` / `LivingEntity#tick` 注入保留为双保险（少数模组会绕过世界 tick 直接 tick 实体）。

## 1.16.2 (2026-09-22)

### 变更 — 无限箭矢不再往背包塞箭
- **旧实现**：临时在背包生成一支带标记的「幽灵箭」供弓弩拉弦，再扫描回收。
  缺点：占格子、可能被丢出或捡走、弓与弩要各维护一套，卸下与切武器时还得扫描回收。
- **新实现**：在**弹药判定层**（`Player#getProjectile` —— 弓、弩以及绝大多数模组远程武器
  统一的「有没有弹药」入口）返回虚拟弹药：
  - **没有箭** → 返回一支普通箭，让武器「有弹药可拉」；
  - **有箭** → 返回该箭的**副本**，原版后续的消耗落在副本上，
    背包里的箭毫发无损，同时保留药箭等箭种类型（射出来仍是原本的箭）。
  - 不需要任何每 tick 逻辑、不需要扫描回收、不污染背包；弩同样被覆盖
    （弩不会触发 `ArrowLooseEvent`，旧实现只能靠幽灵箭兜底）。
- 行为上更贴近原版：力量 / 冲击 / 火矢 / 多重射击 / 穿透等附魔全部交回原版流程处理，
  射出的箭也能正常捡回（不存在刷取问题）。
- 一并移除旧代码：幽灵箭生成与回收、背包满提示、`ArrowLooseEvent` 自定义发射逻辑。

## 1.16.1 (2026-09-22)

### 修复 — 无限箭矢没效果
两个原因，指向同一处遗漏：

- **弓完全不生效（主因）**：弓必须先有弹药才拉得开，事件层的「拦截发射、不消耗箭矢」
  才有机会触发；而备好"幽灵箭"的逻辑此前**只对弩生效**，
  于是弓在没箭时直接拉不开 —— 表现为整个功能失效。现在弓与弩共用同一套备箭逻辑。
- **其它模组的弓弩不覆盖**：判断硬编码为 `Items.BOW` / `Items.CROSSBOW`，
  灾变「诅咒之弓」这类直接继承 `ProjectileWeaponItem` 的武器被漏掉。
  改为按**原版弹药物种**判定（`getAllSupportedProjectiles().test(箭)`）——
  既覆盖模组弓弩，又不会给「枪械类」武器硬塞一根箭进背包。

### 顺带
- 弓的射击改为**两端都拦截**原版流程：客户端不再做「发射 + 消耗箭矢」的本地预测，
  真正的箭只在服务端生成，不会再出现"多出一支只有自己看得到的预测箭"
- 背包放不下幽灵箭时动作栏明确提示（此前是静默失效，容易被再次当成"功能没效果"）

## 1.16.0 (2026-09-22)

### 新增 — 自定义配方（新分类「自定义配方」）
- 配置项 `customRecipes`：每项一个**原版配方 JSON**，直接复用原版解析器
  （`RecipeManager.fromJson`）注入服务器配方表
  - 因此天然支持全部配方类型（有序/无序合成、熔炼、锻造…）以及其它模组注册的 recipe type
  - 非法条目只记录日志并跳过，不影响其余配方，也不会让服务器起不来
- 注入时机：服务器启动自动注入；`/reload` 后重新注入（数据包重载会整体清空配方表）；
  命令 `/eternalheart recipe reload` 可随时按配置重新注入
- 注入后广播完整配方表（`ClientboundUpdateRecipesPacket`），配方书 / JEI 立即可见
- 面板对应行点「编辑」进入**文本列表编辑器**（新增 / 编辑 / 删除 / 清空，回车即追加）——
  这类自由文本不能交给效果选择器，否则点一次「完成」就会破坏内容
- 顺带修掉两个会让该功能实际不可用的限制：
  - 配置快照 JSON 上限 32KB → 256KB（十几条配方就会撑爆旧上限，表现为「保存失败：invalid」）
  - 自由文本列表的元素长度上限 96 → 1024

### 新增 — 时停（新分类「时停」）
- 快捷键 **G**（可在按键设置里改）或命令 `/eternalheart timestop` 发动
- 冻结维度内**除自己以外**的实体：不移动、不执行 AI、弹射物与掉落物一并停住；
  发动者完全不受影响，可照常移动与攻击
- 配置项：`timeStop`（开关）、`timeStopDuration`（秒）、`timeStopCooldown`（秒）、
  `timeStopRadius`（格，0 = 整个维度）、`timeStopAffectPlayers`（是否连其它玩家一起冻结）
- 实现要点：
  - `Entity#tick` 与 `LivingEntity#tick` 头部判定并取消本次 tick
    （生物 AI 不经过 `Entity#tick`，必须单独拦截）
  - 状态用「剩余 tick 计数」，绕开各维度 `getGameTime()` 基准不一致的问题
  - 未激活时判定退化为一次哈希表查询，常态零开销
- 冷却中 / 未开启 / 时停结束均有动作栏提示

## 1.15.0 (2026-09-22)

### 新增 — 冷却缩减（新分类「冷却缩减」）
- 新配置项 `cooldownRatio`：**物品 / 装备技能冷却时长倍率**
  - `1.0` 不变（默认）、`0.5` 减半、`0` 无冷却；值域完全放开（负数按无冷却处理，大于 1 则延长冷却）
- 作用于**所有走原版物品冷却的模组**，例如灾变（L_Ender's Cataclysm）的武器技能
  `player.getCooldowns().addCooldown(this, cooldown)` —— 在最上游缩放时长，因此对所有模组通用
- 实现要点：
  - `ItemCooldownsMixin`：用 `@ModifyVariable` 缩放 `addCooldown(Item, int)` 的时长入参
    （不取消、不重入，倍率 1.0 时与原版行为完全一致）
  - `PlayerCooldownOwnerMixin`：原版 `ItemCooldowns` 是无参构造、**不持有玩家引用**的记账类，
    因此在玩家构造末尾把冷却表与玩家绑定（重生的新实例会自动重绑，无需 tick 轮询）
  - 客户端与服务端同时生效（服务端决定可用性、客户端负责 HUD 冷却显示），两端读同一份配置
  - refmap 已包含新注入点（`addCooldown` → `m_41524_`），正式环境注入正常
- 物品提示新增「冷却时长 ×N%」行（仅在非默认时显示）；中英语言文件同步

### 说明 — 不覆盖自定义冷却
- 若某模组不用原版冷却（例如灾变「沙漠之怒」的 `UseTime` 充能计时器、或自带 NBT 冷却键），
  本功能不会影响它 —— 这类需要按模组单独适配，可按需追加

## 1.14.4 (2026-09-22)

### 修复 — 物品提示出现未替换的 `%s`
- **现象**：tooltip 里「矿石额外掉落 **%s** 次补判（每次 33%）」把 `%s` 原样显示了出来
- **根因**：不是 `%s` 的问题，而是同一行的字面百分号 `33%）` **没有转义成 `33%%）`**。
  带参数的文案会经过 `String.format`，非法的 `%）` 抛 `IllegalFormatException`，
  Minecraft 捕获后**整串原样返回** —— 于是同一行里的 `%s` 也一起没被替换
- **修复**：`tooltip.eternal_heart.fortune`（中英）改为 `33%%`

### 校验 — 静态校验脚本新增两道防线（`tools/check_resources.py`）
- **未转义百分号**：凡含占位符的文案，字面 `%` 必须是 `%%`，否则直接报错；
- **tooltip 参数个数**：解析 `text("键", ...)` 调用，占位符数多于传参时直接失败
  （参数不足同样会让整串原样显示，属同一类隐性 bug）。
- 全量扫描结果：`fortune` 是唯一命中项，其余 tooltip 均正常。

## 1.14.3 (2026-09-22)

### 变更 — 配置面板只验证权限（不再要求佩戴饰品）
- 打开条件从「佩戴饰品 + 单人主机/OP」改为**只验证权限**：单人主机，或 OP（权限等级 ≥ 2）
- 客户端不再做佩戴前置检查与「需要佩戴」提示，直接向服务器请求；
  权限的权威判定在服务端，请求与提交两条路径共用同一校验
- 无权限时服务器返回 `forbidden`，客户端照旧在动作栏提示（文案已更新为只提权限）
- 关闭面板时权限不足仍会禁用「保存并关闭」（沿用既有 `editable` 逻辑）
- 说明：`/eternalheart` 命令的权限要求（等级 2）与面板保持一致，未改动

## 1.14.2 (2026-09-22)

### 修复 — 饰品效果图标「一闪一闪」（每秒被清除后重新添加）
共两处会导致客户端图标消失再出现的「移除 → 重新添加」写法，均已改为原地更新：

- **绑定诅咒不再每秒重写槽位**（首要修复，间隔正好每秒）
  - 现象：解除绑定每 20 tick 检查一次饰品，一旦发现绑定诅咒就**重写槽位**。
    重写会触发 Curios 对该饰品的全量重算与槽位变更事件 —— 整合包里响应该事件的模组
    会把饰品提供的效果「清掉再加回」，表现为每秒闪一次。
  - 修复：清理一次后在物品上打标记（`eternal_heart_unbound`），之后只读不写。
    「随时可取下」依旧由物品自身 `canUnequip = true` 保证，绑定对外依旧无效
    （即使外部再次附上诅咒，也拦不住取下）。
- **永怒降层不再重建效果**（每衰减周期一次）
  - 现象：层数下降时原版 `update` 不支持降级，历史实现走「移除 + 重新添加」——
    客户端先收移除包再收添加包，图标消失后重新插入，表现为周期性闪烁。
  - 修复：降层改用原版 `forceAddEffect`（直接替换实例，客户端只收更新包）
    → 图标原地刷新，层数即时可见且不闪烁。

### 诊断 — 被动效果重建日志（debug 级）
- `OwnedEffects` 新增两条 debug 日志：**被动效果被重建**（含原有剩余时长 → 新时长）
  与 **被动效果被外部移除**。
- 用途：区分「效果闪烁」是我们的维持逻辑重建，还是整合包内其它模组在清除后我们补回 ——
  复现时查看 `latest.log` 中 `[永恒之心]` 开头的行即可定位。

## 1.14.1 (2026-09-22)

### 修复 — 正式环境启动崩溃（mixin 注入失败）
- **现象**：安装 1.14.0 后游戏在启动阶段直接崩溃：
  `InvalidInjectionException: @Inject ... could not find any targets matching 'forceAddEffect' ... No refMap loaded`
- **根因**：1.14.0 构建时 `compileJava` 为 UP-TO-DATE（源码未变，只改了版本号与资源），
  注解处理器没有重新生成 refmap，jar 重新打包时也不再带上它 —— 产物缺少 `eternal_heart.refmap.json`。
  开发环境测不出来：dev 下 mixin 用官方名直接匹配，只有正式环境（SRG 名）才依赖 refmap
- **修复**：
  - 把 refmap 声明为 `compileJava` 的**输出**：文件缺失时 Gradle 判定任务非最新并重跑，
    从机制上消除「jar 重打但 refmap 没重建」的增量构建陷阱
  - jar 任务**显式**把 refmap 打进产物根目录（`from` + 去重策略），不再依赖 MixinGradle 的注入时机
  - 新增**产物自检**：`build` 结束时校验产物必须包含 `eternal_heart.mixins.json` 与
    `eternal_heart.refmap.json`，否则直接构建失败 —— 同类问题不会再流出
- 说明：1.14.0 的功能本身没有问题（开发环境 15/15 测试通过），仅产物缺 refmap；请使用 1.14.1

## 1.14.0 (2026-09-22)

### 新增 — Mixin 基础设施（对齐成熟模组的底层注入能力）
- 引入 MixinGradle + 注解处理器，编译期生成 `eternal_heart.refmap.json`（官方名 → SRG 名），
  生产环境注入有完整保障；jar 内自动携带 mixin 配置与 refmap
- 配置声明 `required=true` + `defaultRequire=1`：注入点不匹配会**直接暴露**，而不是静默失效

### 新增 — 效果硬拦截：堵住绕过事件层的路径
- 原版 `LivingEntity#forceAddEffect(MobEffectInstance, Entity)` 会**无视事件**强制写入效果——
  这是纯事件层方案（`MobEffectEvent.Applicable`）唯一覆盖不到的路径
- 新增 `LivingEntityMixin` 在该方法头部直接取消：佩戴 + 规则命中时**零窗口期**阻断
- 三级防线定型：事件层（常规 addEffect）→ Mixin（forceAddEffect）→ 周期清扫（极端路径兜底）

### 修复 — 配置类型校验（内核级缺陷）
- `ConfigValues.validate` 原先只校验"数值有限 + 范围"：给整型项传 `1.5` 这类**类型不符**的值会被
  写入，随后在强转处崩溃并污染全局配置状态
- 现在严格按配置项类型校验（Bool / Int / Double / Enum / List），类型不符直接拒绝

### 修复 — gametest 环境打通
- MixinGradle 顺带修好了开发环境里第三方模组（Curios）refmap 无法重映射的问题，gametest 首次可正常启动
- **All 15 required tests passed**（含新增的负面效果规则集测试）
- 修正两个过期断言（配置上下限放开后 `DODGE_CHANCE=1.1`、`FURY_MAX_STACKS=0` 已是合法值）
- 新增 `-PnoMineFargo` 开关：`mine_fargo` 内嵌的 expandability 在 dev 环境 mixin 不兼容，
  跑测试时用它把该依赖移出 classpath —— `gradlew runGameTestServer -PnoMineFargo`

## 1.13.0 (2026-09-22)

### 重构 — 负面效果拦截（`Debuffs` 全面重写）
- **配置变更时编译，判定时零分配**：黑 / 白名单的字符串 ID 在变更时一次性解析为
  `MobEffect` 引用集合（fastutil 开放寻址），判定从「字符串拼接 + 两次列表线性查找」
  变为「2 次 O(1) 集合查询 + 1 次类别判断」——热路径无字符串分配、无遍历
- **中性效果的负属性预计算**：建集时一次性分类注册表里「中性但带负属性」的效果，
  判定不再遍历属性修饰符
- **变更检测两条路**：配置 List 引用变化、效果注册表尺寸变化都会自动重建规则集；
  `invalidate()` 保留为显式失效入口
- **新增 `debuffGuard` 开关**（默认开）：拦截功能拥有独立开关；`debuffClearInterval` 现在
  只表示「周期清扫间隔」（0 / 负 = 只拦新施加的效果，不做周期清扫），不再兼任总开关
- **佩戴瞬间立即净化**：不必等第一个清扫周期（默认最长 3 秒）
- **黑名单可覆盖被动效果**：永恒之心维持的被动效果一旦被列入黑名单即停止维持
  （避免「清扫掉又立刻加回」的拉锯）
- **可观测**：新增 `/eternalheart debuff check <effect>`——显示某效果是否会被拦截，
  以及**判定来源**（白名单 / 黑名单 / 负面分类 / 中性负属性）

### 性能 — 配置读取 O(n) → O(1)
- `ConfigValues.get` 是全局最热路径（各功能每 tick 调用数百次）；原先在联机环境下
  每次线性扫描 130+ 项配置表，单 tick 累计上万次比较。现改为身份哈希索引（IdentityHashMap）
- 该优化对**所有功能**生效（拦截、光环、战斗、机动、便利……）

### 测试
- 新增 gametest：白名单优先、黑名单强制、判定来源、规则自动重建、黑名单覆盖被动效果

## 1.12.0 (2026-09-22)

### 新增 — 诡厄巫法：启示录兼容：自动写入「天启」豁免名单
（`revelationfixWhitelist`，模组集成分类，默认开启）

- 下界亚波伦「末日终结」的夺取行为由 RevelationFix 的**公开配置项**控制：
  `config/revelationfix/revelationfix-common.toml` → `["The Apocalypse"].whitelistItems`
- 本模组在配置加载阶段会把 `eternal_heart:eternal_heart` **幂等**写入该名单：
  从源头豁免，**不会出现"被夺走后归还"造成的重复物品**；关闭开关则移除我们写入的条目
- **合规边界（RevelationFix 为 ARR 协议）**：
  - 只读写其**公开配置文件**（等价于整合包作者手工加一行）
  - 不修改 / 不反编译 / 不反射其模组本体；其它条目与注释原样保留
- 命令：`/eternalheart compat revelationfix [on|off]`（查看状态 / 开启 / 关闭并立即同步）
- 说明：若 RevelationFix 的配置加载早于本模组，条目会在**下次启动**生效（日志会提示）；
  与既有的「饰品防夺」（运行期收回）形成双保险，可单独开关

## 1.11.0 (2026-09-22)

### 新增 — 饰品防夺（`curioSeizeGuard`，防护机制分类，默认开启）
针对「诡厄巫法：启示录」下界亚波伦「末日终结」阶段的夺取机制（默认夺走非豁免玩家的饰品与装备）：

- **检测被夺**：监听 Curios 的 `CurioChangeEvent`——饰品被移出槽位且
  「玩家侧一无所获」（背包 / 光标 / 附近掉落物都没有）时判定为被夺取；
  玩家主动卸下（物品进背包 / 掉落物）不受影响，绝不妨碍正常操作
- **立即收回**：下一 tick 放回原槽位（不可用则退回背包），同时重建属性加成 + 动作栏提示
- **属性防还原**：新增低频校验（每 40 tick，相位错开）——其它模组的
  「属性还原 / 清洗」移除我们的修饰符时自动重建，饰品加成持续生效
- 与既有防护的关系：死亡流程由「死亡不掉落」接管（按 `isAlive` 隔离，互不冲突）

### 注意事项
- 若亚波伦战后仍有归还箱 / 饰品存储水晶（原模组记录的被夺物品），其中可能也有一份饰品，
  建议销毁多余的那份以免重复；不需要此功能时可在配置中关闭「饰品防夺」

## 1.10.1 (2026-09-22)

### 修复
- **切换维度后无法飞行**：
  - 根因：切换维度时客户端会重建玩家实例，飞行能力完全依赖服务端的
    `ClientboundPlayerAbilitiesPacket`；而授权逻辑是「mayfly 为 false 才授权并发包」——
    服务端状态没有变化就不会再发包，客户端于是停留在默认的 mayfly=false（无法起飞）
  - 修复：新增 `MobilitySystem.resyncFlight`，在 `PlayerChangedDimensionEvent` /
    `PlayerRespawnEvent` / `PlayerLoggedInEvent` 三个时机强制重新授权并补发能力包
  - 追加低频补发（每 100 tick，相位错开）：防御客户端被其它机制重置 mayfly 后长期失同步
  - `granted` 标记丢失时会自动重建（旧存档/其它途径清空标记时，避免卸下后飞行能力残留）
  - 回收逻辑改为「只关我们自己授予的能力」：重同步时绝不误关其它模组给的飞行

## 1.10.0 (2026-09-22)

### 新增 — 饰品守护：死亡不掉落 + 无视绑定（防护机制分类，2 项开关）
- **`curioKeepOnDeath`（饰品死亡不掉落，默认开启）**
  - 死亡瞬间就从 Curios 槽位取出饰品并快照（物品 + 原槽位 identifier/index），
    写入内存与玩家持久化数据（覆盖「死亡到重生之间服务器重启」的极端情况）
  - 槽位被提前清空 ⇒ Curios 的死亡掉落流程「无物可掉」，不存在竞态
  - `LivingDropsEvent` 兜底删除任何被其它路径掉出的饰品
  - 重生时（在 Curios 复制数据之后执行）按「原槽位 → 第一个可用空槽 → 背包 → 脚下」归还，绝不丢失
  - **独立于 `keepInventory`**：即使死亡保留物品关闭，饰品依然不掉落
- **`curioUnbind`（无视绑定，默认开启）**
  - 每 20 tick 检查并清除饰品上的绑定诅咒附魔（无论由哪个模组附上），并给出动作栏提示
  - 物品 `canEquip` / `canUnequip` 恒为 true：Curios 层永远允许取下
  - 命令 `/eternalheart curio unequip [player]`：直接写槽位绕过一切锁定（绑定 / 硬锁模组的最终兜底）
- 物品提示新增「死亡不掉落（重生归还）」「无视绑定，随时可取下」

### 实现核对
- Curios 的死亡掉落走 Forge 的 `LivingDropsEvent`（`CuriosEventHandler#playerDrops`），
  因此「提前取出 + 掉落过滤 + 重生归还」三层覆盖所有掉落路径
- 已按 Curios 5.14.1 实际 API 核对：`CuriosApi.getCuriosInventory` / `findFirstCurio` /
  `SlotResult.slotContext` / `IDynamicStackHandler.setStackInSlot`（强制取下与归还均绕过 canUnequip）

## 1.9.0 (2026-09-22)

### 新增 — 属性系统支持任意模组属性（自定义属性）
- 新增配置项 `extraAttributes`（「扩展属性」分类）：为**任意已注册属性**（含其它模组）添加修饰符
  - 书写格式：`"<属性ID> <数值> [操作类型]"`，空格分隔（例：`"pehkui:base_scale 0.25 multiply_total"`）
  - 操作类型：`addition`（默认）/ `multiply_base` / `multiply_total`
  - 数值语义为修饰符原始 amount（不做总倍率换算），适配各模组属性的量纲
  - 未注册的属性（模组未安装）在加载时静默忽略，不影响其余条目
- **面板可视化编辑**：配置项行内点「编辑」进入属性选择器——
  - 列出全部已注册属性（原版 / Forge / 其它模组），左侧色条标记已配置项
  - 支持按本地化名称或 ID 搜索
  - 点击属性行进入数值弹层：数值输入 + 操作类型循环切换 + 移除
  - 与内置属性重复的属性同样可选（UUID 不同，各自生效互不覆盖）
- **命令入口**（OP / 单人主机，Tab 补全全部属性 ID）：
  - `/eternalheart attribute set <attribute> <value> [operation]`
  - `/eternalheart attribute remove <attribute>`、`list`、`clear`
- 实现要点：
  - 每条自定义属性的 UUID 由属性 ID 确定性派生——同一属性永不重复叠加
  - 热刷新按「全注册表 × 确定性 UUID」先清后建，配置里删掉的旧条目也会被移除
    （无状态实现，不需要记录每个玩家上一次应用过哪些属性）
  - 装备时只登记实体确实拥有实例的属性，避免给不支持的实体挂空属性
  - 物品提示新增自定义属性行（最多 3 条，其余折叠显示）；中英语言文件同步

## 1.8.0 (2026-09-22)

### 新增 — 光环独立开关（4 项）
- 伤害光环、凋零光环、和平光环、生长光环各自新增独立开关
  （`damageAuraEnabled`、`witherAuraEnabled`、`peaceAuraEnabled`、`growthAuraEnabled`，默认开启）
- 开关关闭时整体停用（范围设置保留）；开关与范围需同时满足才生效
- 物品提示中的光环行同步遵循开关

### 新增 — 负面效果黑 / 白名单的可视化编辑与命令入口
- **配置面板内置编辑器**：黑名单 / 白名单不再是只能手改 toml 的「隐形配置」——
  面板行内显示当前项数，点击「编辑」进入效果选择器：
  - 列出全部已注册效果（含其它模组），左侧色条区分 有益 / 负面 / 中性
  - 支持按本地化名称或 ID 搜索；点击任意行即加入 / 移出
  - 白名单优先于黑名单（与判定逻辑一致），改动随面板统一保存
- **命令入口**（OP / 单人主机，Tab 补全全部效果 ID）：
  - `/eternalheart debuff list` 查看两个名单
  - `/eternalheart debuff blacklist add|remove <effect>`、`clear`
  - `/eternalheart debuff whitelist add|remove <effect>`、`clear`
  - 修改后立即保存并同步在线玩家（与面板保存共用同一条发布链路）

### 新增 — 扩展属性（7 项）
- 游泳速度（`swimSpeed`）、飞行速度（`flyingSpeed`）：总倍率，默认 1.0
- 攻击击退（`attackKnockback`）、实体交互距离（`entityReach`）、方块交互距离（`blockReach`）、
  台阶高度（`stepHeight`）、名牌距离（`nametagDistance`）：附加，默认 0
- 属性表改为延迟解析（`Supplier`）：Forge 自注册属性在注册完成后才取值，避免注册期崩溃
- 物品提示新增扩展属性行（仅在非默认值时显示）；中英语言文件同步

## 1.7.4 (2026-09-22)

### 修复
- **数值很大时配置面板输入框显示为空**：
  - 根因：上一版为超大数值引入了科学计数法显示（如 `1E13`、`1.798E308`），
    但输入框的字符过滤器只允许数字与小数点 —— 而 `EditBox.setValue` 在过滤器返回
    false 时**直接不赋值**，于是输入框变成空白（数值其实已经保存成功，只是看不见）
  - 修复：过滤器改为允许科学计数法与符号中间态
    （`1E13`、`1e-5`、`1.798E308`、`+3`、`-`、`.` 均可输入），与解析逻辑
    （`Double.parseDouble`）保持一致
- 科学计数法阈值由 `1E7` 提高到 `1E9`：`1000000` 这类常见数值仍按普通写法显示，
  方便手工编辑；只有 10 亿以上的数值才用 `1E10` 这种紧凑写法

## 1.7.3 (2026-09-21)

### 新增 — 突破其它模组的限伤（战斗系统分类，2 项开关）
- **`pierceDamageCap`（突破单次伤害上限，默认开启）**
  - 原理：本模组的加伤在 `LivingHurtEvent` 阶段完成，此时把期望伤害记录下来；
    伤害继续走到**最后一道** `LivingDamageEvent`（LOWEST 优先级，排在其它模组之后）时比对，
    如果被别的模组压到上限以下就恢复回来
  - **只提升不压低**：其它模组的合法增伤（暴击词缀等）不会被我们改回去
  - **只与本 tick 的伤害配对**：伤害被无敌帧拦下（事件不触发）时记录自然过期，
    不会把上一次的期望值用到下一次攻击
  - 说明：期望值是护甲结算前的数值，因此开启后**同时会穿透目标护甲 / 抗性 / 吸收的减伤**；
    想要只突破限伤而保留护甲结算的话关掉本项即可
- **`pierceImmunity`（突破免伤 / 真实伤害，默认关闭）**
  - 目标用「取消伤害事件」实现完全免疫时，改用真实伤害直接扣血（跳过整条伤害管线）
  - 覆盖两类拦截：`LivingDamageEvent` 取消（免伤型）、`LivingAttackEvent` 取消
    （BOSS 无敌阶段 / 闪避类模组）——后者会用增伤管线自行算一遍期望伤害再扣血
  - 仅对非玩家目标生效，避免误伤队友；开启后会连带无视不死图腾这类保护
- 物品提示新增「突破其它模组的限伤」，配置项中英双语名称同步

## 1.7.2 (2026-09-21)

### 变更 — 配置下限全面放开（可以填负数）
所有数值项下限统一改为 `-Double.MAX_VALUE` / `Integer.MIN_VALUE`，
配置面板输入框本就允许负号（48 字符内），现在负数会真实生效：

| 配置类别 | 负数语义 |
|---|---|
| 属性加成（攻击、攻速、生命、护甲、韧性、移速、幸运） | 反向削弱自己（负生命 = 自减生命上限） |
| 伤害减免 / 弹射·魔法·爆炸抗性 / 受伤上限 | 负数 = 放大承受的伤害 |
| 摔落伤害保留 | ≤0 = 完全免疫摔落 |
| 反伤 / 溅射比例 / 光环伤害 / 额外打击 | 负 = 反过来治疗对方 |
| 生命回复 / 击杀回血 / 生命窃取 | 负 = 持续掉血 / 击杀扣血 / 吸血变失血 |
| 概率类（闪避、反射、点燃、斩杀、掉落保护等） | 负 = 永不触发 |
| 范围类 / 间隔类 / 时长类 | 负 = 关闭该功能，或按"最频繁"处理（间隔负 = 每 tick 一次） |
| 永怒层数 / 击杀叠层 | 负 = 层数恒为 0 / 负加成即掉层 |
| 自动火把光照阈值 / 抢夺等级 / 爆炸威力 | 负 = 永不放置火把 / 按 0 处理 / 不产生爆炸 |

### 加固 — 负数专属保护
- **新增 `Health` 统一入口**：负回复 / 负吸血无法走原版 `heal()`，只能走 `setHealth`，
  而这会被「血量防篡改」判定成被改血并回滚（表现为"填了负数没反应"）；
  现在这条路径会主动同步生命审计基线，负数真实生效
- **抢夺等级钳制到 ≥ 0**：负等级会让原版战利品表的 `nextInt(looting + 1)` 抛异常
- **击杀爆炸威力钳制到 ≥ 0**：负半径对原版爆炸没有意义
- **连锁挖掘上限改 long 计算**：`Integer.MIN_VALUE - 1` 会反向溢出成巨正值，
  导致一次连锁请求遍历数十亿方块（卡死），现在钳制到 0 与安全上限
- **掉落经验钳制到 ≥ 0**：负倍率不再扣经验（按 0 处理）

## 1.7.1 (2026-09-21)

### 变更 — 配置上限全面放开（真·无上限）
- **数值 / 倍率 / 概率类**：上限统一改为 `Double.MAX_VALUE`
  （攻击、攻速、生命、护甲、韧性、移速、幸运、击退抗性、全部减伤与抗性、
  摔落保留、反伤、受伤上限、回复、急迫、闪避、弹射反射、低血阈值与倍率、
  Boss 增伤、斩杀阈值、溅射比例、额外打击、发光增伤、点燃概率、护盾充能与上限、
  自动喝药阈值、闪电反射、各类光环伤害、永怒全部参数、经验倍率、致死保护保留比例）
- **时长 / 等级 / 层数 / 计数类**：上限统一改为 `Integer.MAX_VALUE`
  （清除负面效果间隔、额外打击冷却、点燃时长、速度爆发时长与等级、伤害光环间隔、
  复活冷却、自动钓鱼间隔、永怒层数/衰减/击杀加成、致死保护冷却、掉落物保护时长、
  自动火把光照阈值——大于 15 即等于永不放置火把）
- **保留引擎安全上限的项（再大会让服务器长时间阻塞，属物理约束非保守设定）**：
  | 项目 | 原上限 | 现上限 |
  |---|---|---|
  | 各类实体扫描范围（磁铁/发光/凋零/和平/伤害光环/溅射/连锁/破盾击退/洞穴高亮） | 128 | **512 格** |
  | 生长光环范围（每周期遍历 (2r+1)²×7 个方块） | 16 | **64 格** |
  | 连锁挖掘最大方块数 | 1024 | **65536** |
  | 生长光环每周期催熟数 | 1024 | **4096** |
  | 额外时运补判 / 额外抢夺等级 | 100 | **1000** |
  | 击杀爆炸威力 | 16 | **256** |

### 加固 — 上限放开后的安全钳制（避免副作用）
- **减伤管线钳制到 0**：减免比例允许超过 100%，但结果不会变成负数
  （负数伤害在原版会反过来给玩家回血，等于把"减免"变成"治疗"）
- **点燃秒数钳制**：原版按「秒 × 20」写 tick，超大秒数会 int 溢出
- **掉落经验钳制到 1 亿**：极端倍率下 `double → int` 会饱和到 MAX_VALUE，
  导致原版升级循环耗时极长
- **永怒叠层改用 long 累加**：`层数 + 加成` 在放开上限后可能越过 int 上限
- **数值显示改用科学计数法**（≥ 1E7 或 < 1E-4）：配置面板与物品提示不再输出
  300 多位数字；面板显示值 ×100 / ×20 溢出为 Infinity 时回退到有限最大值

## 1.7.0 (2026-09-21)

### 新增 — 防护机制（Aegis）
- **血量防篡改**（`healthGuard`）：每 tick 审计生命值。只有走过伤害管线的掉血才被接受，
  任何经由 `setHealth` 之类旁路手段的直接削血（被其它模组偷血、被机制强制改血）都会
  被立即回滚。判定依据是「期间有没有合法伤害事件」，因此正常受伤、中毒、饥饿、虚空
  伤害全部放行，不会误判
- **生命上限保护**（`maxHealthGuard`）：佩戴瞬间记录最大生命值基线，此后上限被外部削减
  （偷取 / 封印生命上限）时用固定 UUID 的补足修饰符补回差额；外部恢复或换上更强装备后
  补足自动撤销，不会叠加
- **致死保护**（`lethalGuard` / `lethalGuardRatio` / `lethalGuardCooldown`）：在最终伤害
  阶段拦截一击必杀，把伤害下调到只扣到「保留生命比例」为止，并进入冷却；触发时给出
  动作栏提示与图腾音效。`/kill`、虚空等无视无敌的伤害不在保护范围（避免管理员无法处理）
- **死亡保留物品 / 经验**（`keepInventory` / `keepExperience`）：死亡时先把物品栏与经验
  备份，再立即清空，使原版死亡掉落流程无物可掉（掉落物不会被他人或收集类机制收走），
  重生时原样归还；备份同时写入玩家持久化数据，服务器在死亡与重生之间崩溃也不会丢物品
- **掉落物归属保护**（`itemGuard` / `itemGuardTicks`）：自己丢出的物品在保护时长内只有
  本人能拾取，防止被其它玩家或第三方磁铁 / 收集机制收走
- 配置面板新增「防护机制」分类（9 项）+ 物品提示新增金色防护行，含中英文案

### 重构 — 用更高阶实现重写全部功能模块
- **新增运行时基础设施 `core` 包**：
  - `PlayerScoped` — 全项目统一的玩家作用域状态容器（fastutil 开放寻址哈希表，
    取代散落的 `HashMap` / `ConcurrentHashMap`，并提供统一清理入口）
  - `Ticker` — 周期任务门控，按玩家 UUID 派生相位：多人服务器上不同玩家的重任务
    不再集中在同一 tick，消除周期性 tick 时间尖峰
  - `Scan` — 统一的实体查询与 Boss 判定（谓词复用、零分配、消除各模块重复的 AABB 与 lambda）
  - `Debuffs` — 负面效果判定的唯一入口，结果按效果缓存（配置变更自动失效），
    判定从「注册表查表 + 字符串拼接 + 两次 O(n) 列表查找」降为一次哈希查找
- **状态跟踪层**：`EquipTracker` 改用 fastutil 状态表并补充服务器停止清理；
  `OwnedEffects` 改为内存镜像 + 仅在账本变更时写回 NBT（热路径不再做 2~4 次复合标签读写）；
  `OwnedShield` 改为内存记账、仅在数值变化时落盘（满血充能期 NBT 写入降为 0）
- **永怒系统**：层数变化只置脏标记、同一 tick 末尾统一同步 —— 一次连击（攻击 +1、
  暴击 +2、击杀 +N）的多次效果重建合并为一次；满怒时零重建；升层直接 `addEffect`
  （原版内部走 update 并自动重算属性，无需移除重建），仅降层才移除重建
- **防御系统**：伤害处理拆成 `immuneTo / mitigate / retaliate` 等纯函数便于推演；
  周期清扫零分配（无负面效果时不构造任何集合）；免疫判定改为静态伤害类型表
- **战斗系统**：额外打击冷却从「每击写目标实体 NBT」改为 fastutil 长整型表 + 惰性清理
  （实体死亡后自动回收，不泄漏）；级联伤害防护升级为深度计数（嵌套时内层退出不会
  误解除外层保护）；剥离增益改为零分配遍历
- **生存系统**：幽灵箭改用「是否生成过」的内存标记短路 —— 常态下不再每 tick 扫描背包
  36 格；自动喝药由两遍扫描合并为一遍；灵魂绑定支持跨维度重生点（历史实现会错位传送）
- **机动系统**：水 / 岩浆行走的流体查询由最多 4 次合并为 2 次，选中判定收进单一谓词；
  移除从未写入的旧下潜标记等死代码
- **便利系统**：连锁挖矿改为 long 编码坐标 + fastutil 队列 / 集合 + 复用查询坐标，
  密集矿脉下 BFS 从「每节点装箱坐标并新建邻域 BlockPos」变为零对象分配；
  磁铁改用距离平方比较（每 tick 每个掉落物省一次开方）；待处理连锁与火把槽位缓存
  改由 `PlayerScoped` 托管（原为裸 HashMap，退出时只在部分路径清理）
- **光环系统**：伤害光环与凋零光环同 tick 到期时合并为一次范围查询（各自按半径过滤）；
  生长光环按需收集候选（关闭自动补种时不收集成熟作物），常见情况零分配
- **调度层**：`FeatureManager` 改为不可变数组注册 + 错误日志限流（同一模块同一阶段最多
  打印 5 条，避免异常时刷屏）并额外捕获 `LinkageError`；`AttributeFeature` 改为声明式
  属性表（8 条属性原先在「装备注册」与「热刷新」两处各写一遍，共 16 段重复代码，现共用一张表）

### 修复
- **切换物品时飞行状态被重置**：佩戴判定原先把「Curios 数据读不到」与「玩家没佩戴」
  都当成未佩戴 —— 切换手持物品、维度切换、同步间隙只要出现一次查询失败就会被记入
  卸下计数，客户端随即停掉表现层（内置滑翔立刻消失）、服务端在窗口结束后回收飞行能力。
  现在两处修正并叠加：
  1. **数据不可用 ≠ 未佩戴**：`getCuriosInventory` 返回空时保守维持「仍佩戴」，
     只有「数据可读且确实找不到饰品」才算卸下；
  2. **客户端同样启用卸下防抖**（与服务端一致的确认窗口），表现层不再被瞬时误判中断；
  3. 卸下确认窗口由 5 tick 放宽到 10 tick，并补充 debug 日志（判定卸下 / 回收飞行时打印）
- 熔炼结果缓存在数据包重载后不失效（历史实现只在首次查询时缓存，配方被数据包改写后
  仍返回旧结果）→ 现在数据包同步时自动清空缓存
- 配置保存 / 文件重载后，负面效果判定缓存与永怒效果属性同步一并刷新

## 1.6.0 (2026-09-21)

### 重构
- **核心框架重构 — 新增 `EquipTracker` 装备状态跟踪器**（全项目唯一装备判定入口）：
  - **卸下防抖（关键修复）**：服务端连续 5 tick 查不到饰品才判定为真卸下。
    原来维度切换 / 死亡重生 / Curios 数据同步间隙的 1~2 tick 瞬时误判会立即触发
    卸载清理（回收飞行、移除被动效果、重置永怒），表现为 **「飞着飞着突然掉下来」**；
    现在误判会被防抖窗口拦截
  - **性能优化**：原来每个事件处理器都直接查询 Curios 饰品栏（一次玩家操作可能触发
    十几次查询），现在每 tick 只查询一次、其余全部走缓存
  - 玩家退出时自动清理状态缓存
- **飞行系统重构**：
  - **移除「无 Caelus 自动回退创造飞行」**（静默替换用户选择、且创造飞行比鞘翅更强，
    违背配置意图）；「飞行模式」的选择现在严格生效
  - **内置自定义滑翔**：未安装 Caelus 时，鞘翅模式改用内置实现 ——
    自由落体（下落速度 < -0.6）时自动缓降（平滑衰减至 -0.22 格/tick 并沿视角前进，
    水平速度上限 1.1 格/tick），潜行取消滑翔、滑翔期间免摔伤；
    不依赖原版鞘翅判定与 Mixin。装了 Caelus 仍优先使用真鞘翅
- **负面效果双层判定统一**：预防层（`MobEffectEvent.Applicable`）与周期清扫层
  共用 `DefenseSystem.isDebuffBlocked()` 单一判定 ——
  修复历史问题「白名单只在清扫层生效、黑名单效果会先被加上再清除」；
  同时去掉易出错的缓存字段，直接读取配置

### 修复
- **FTB Ultimine 激活后不释放**：连锁挖掘调用后从不关闭，导致后续所有挖掘都被
  Ultimine 接管成连锁；现在激活后 2 tick 自动释放（本此连锁已在同一破坏事件内完成）
- **斩杀伤害硬编码 9999**：改为目标最大生命值 ×10 结算，任何血量的目标都能秒杀
- **额外时运对非矿石方块的凭空复制**：时运掉落复制现在只对矿石生效
  （与原版时运的实际作用范围一致）
- **掉落接管范围收窄**：自动冶炼/额外时运仅在「确有处理需求」时取消事件并手动接管
  （无掉落、无可冶炼配方、非矿石的方块交还原版），减少与其他模组的兼容冲突

### 优化
- 洞穴高亮实体扫描由每 tick 降为每 10 tick（发光效果持续 400 tick，无需高频刷新）
- 生长光环扫描高度收窄为玩家上下各 3 格（原为整立方体，范围 12 时从 15625 方块
  降到 4375，减少 72% 方块查询）
- 幽灵箭定位改用 `Inventory.getFreeSlot()`（原为逐格扫描 36 格）
- 配置注释更新：飞行模式说明补充内置滑翔行为

## 1.5.1 (2026-09-21)

### 新增
- **配置面板打开限制**（双重条件，均满足才允许打开）：
  1. **必须佩戴永恒之心**——未佩戴时按快捷键提示「需要佩戴永恒之心才能打开配置面板」
  2. **仅单人游戏（含局域网主机）或 OP（权限等级 ≥ 2）**——联机普通玩家按快捷键
     提示「联机时只有 OP 可以打开配置面板」
- 被拒绝时给出动作栏提示（金色前缀 + 黄色文字），不再「按了没反应」
- 设计说明：联机时普通玩家改配置只影响自己客户端本地副本、不会同步服务器，
  直接禁止可避免「改了不生效」的误导；单人让戴饰品的玩家自由调整

## 1.5.0 (2026-09-21)

### 重构
- **飞行系统重构**：`飞行模式`（FLIGHT_MODE）成为唯一控制源，移除冗余的「创造飞行」布尔开关
  - 原因：原来两个配置语义重叠导致开关失效——把「创造飞行」关闭后，只要「飞行模式」
    还是创造模式就依然能飞，用户看到的是「开关关了还能飞」
  - 现在：配置面板中只有一个「飞行模式」三态切换——**创造模式 / 鞘翅滑翔 / 关闭**，选什么就是什么
  - 代码整理：`handleFlight` 拆分为 `applyCreativeFlight` / `applyElytraFlight` /
    `revokeAllFlight` / `revokeCreativeFlight` 四个职责单一的方法；
    删除 `resolveFlightMode` 兼容逻辑与未使用的飞行意图 NBT 键；`onUnequip` 统一回收

### 新增
- **鞘翅模式回退保护**：未安装 Caelus 时选择「鞘翅滑翔」会自动回退为创造飞行
  （只打印一次警告），避免「选了鞘翅却完全不能飞」

### 兼容性说明
- 移除配置项 `creativeFlight`：旧配置文件中的该项会被忽略（不会报错）
- 若你之前把「飞行模式」设为「关闭」、依赖旧的创造飞行开关飞行，请打开配置面板
  （+ 键）把「飞行模式」切回「创造模式」即可恢复

## 1.4.3 (2026-09-21)

### 修复
- **数值输入框点击无反应（点不到、无法输入）**：
  - 根因：渲染循环内有「每帧统一隐藏行控件」的清理代码，其中包含 `setFocused(false)`——
    用户点击输入框聚焦后，**下一帧渲染立即把焦点清掉**，输入框永远无法保持焦点
  - 修复：旧行控件的隐藏与焦点清理移到 `applyFilter()`（仅在切换分类/搜索等过滤变化时执行一次）；
    渲染循环内不再做全量焦点清理，仅对视口外的行做隐藏清理

### 变更
- **数值行去掉滑块**，改为加宽的数值输入框（110px，14 位长数字完整可读）
- **输入框首次点击自动全选内容**：直接键入即可覆盖旧值（再次点击可正常定位光标）
- 清理滑块相关死代码：数值范围表（RANGE）、rangeFor / niceMax / toNorm / clamp01、CfgSlider 类

## 1.4.2 (2026-09-21)

### 修复
- **数值输入框无法移动光标、只能改末尾数字（表现为数值被无限追加膨胀）**：
  - 根因：输入框 responder 内回写 `setValue()`，而 `EditBox.setValue` 会把光标强制移到末尾，
    导致插入点无法移动、正在输入的中间态（如 `1.`）被实时格式化覆盖，末尾追加则不断膨胀
  - 修复：编辑过程中不再回写输入框（仅单向同步滑块位置）；失焦或按回车时统一规整格式
  - 新增字符过滤：输入框只接受数字与一个小数点，拒绝粘贴/输入的垃圾字符
  - 新增回车提交：数值框按回车即失焦并格式规整

- **切换左侧分类后右侧出现上一批控件残留（新旧行重叠）**：
  - 根因：行控件只在「当前过滤结果」循环里被隐藏，切分类后不在新列表中的旧行控件从未隐藏
  - 修复：每帧渲染前先把全部行控件统一置为隐藏，再只为当前可见行开启

## 1.4.1 (2026-09-21)

### 修改
- **默认快捷键 H → +**：打开配置面板的默认按键由 H 改为 `+`（主键盘 =/+ 键位）
  - 已自定义过按键的玩家不受影响（按键绑定仍以用户设置为准），新安装/重置后默认为 +
  - 同步更新配置面板标题副文案「按 + 唤出」

## 1.4.0 (2026-09-21)

### 重构
- **H 键配置面板全面重构**：从全屏拉伸列表改为屏幕中央的精致面板
  - 视觉：深蓝夜空底色 + 暗金双线边框、header 中心发光分隔线、交替行底色、行悬停高亮、右侧金色滚动条
  - 布局：内容区改用 scissor 裁剪，滚动更平滑，不再整行跳隐

### 新增
- **搜索功能**：面板顶部搜索框实时过滤，支持按中文显示名 / Java 字段名 / toml 键名 / 分类名检索，无结果时显示空状态提示
- **分类导航**：左侧分类栏自动从配置 path 提取（19 个分类 + 全部），点击分类只显示该类配置，再次点击返回全部；分类过多时左栏可独立滚动
- **搜索与分类联动**：输入搜索词自动切回全局视图；点选分类自动清空搜索词
- **开关按钮美化**：开启显示绿色「● 开启」、关闭显示灰色「○ 关闭」
- **细节交互**：搜索框聚焦时按 ESC 先退出焦点而非直接关屏；窗口缩放后保留搜索词与分类选中状态；底部状态栏动态显示当前视图信息

### 改进
- 数值滑条范围补充：额外打击冷却 (EXTRA_HIT_COOLDOWN)、自动钓鱼间隔 (AUTO_FISH_INTERVAL)
- 行名超宽自动截断（省略号），窄屏幕下不再溢出

## 1.3.1 (2026-09-21)

### 移除
- **爬墙**：移除贴墙爬升功能，删除 `wallClimb` / `wallClimbSpeed` 配置项
- **双击 W 冲刺**：移除冲刺功能（含冲刺伤害/无敌帧/冷却），删除 `dash` / `dashForce` / `dashInvulnTicks` / `dashDamage` / `dashCooldown` 共 5 项配置及整个「冲刺 (Dash)」配置分类
- **宝箱磁铁**：移除自动收集附近箱子物品功能，删除 `autoOpenChest` / `autoOpenChestRange` 配置项
- 同步清理：物品 tooltip 描述、H 键配置面板条目（中文名映射 + 数值范围）、中英语言文件

## 1.1.9 (2026-07-03)

### 改进
- **配置文件数值范围大幅拓宽**：所有 `Int` 上限改为 `Integer.MAX_VALUE` (2147483647)，所有 `Double` 上限改为 `Double.MAX_VALUE`，不再限制玩家自定义数值上限
  - 属性：攻击伤害/速度/生命/护甲/韧性/击退抗性/移动速度/幸运
  - 防御：伤害减免/摔落/反伤/爆炸减免/受伤上限
  - 回复、战斗强化、采集强化、生存保障
  - 环境适应、光环范围、永怒系统、经验倍率
  - 探索便利、战斗生存增强、机动便利
- 部分下限也同步放宽：不死冷却 0（可禁用）、自动钓鱼间隔 1 tick、冲刺力度 0（关闭）

## 1.1.8 (2026-07-03)

### 修复
- **水上行走/岩浆行走最终修复**：从 `setPosRaw`（不更新碰撞箱，被流体物理覆盖）改为 `setPos`（更新碰撞箱）
  + 深度下陷（>0.3 格，如从高处跌落）时自动用 `teleportTo` 完全重置状态
  + 轻微下沉（>0.005 格）用 `setPos` 平滑修正，无画面抖动
  + 归零垂直动量 + `setOnGround(true)` 在 `travel()` 之前拦截流体下沉

- **爬墙修复**：不再依赖服务端不可靠的 `horizontalCollision` 和 `player.zza`
  + 改为方块检测：检查玩家面前 0.4 格处脚部+胸部两层是否有实心方块
  + 移动方向判断：优先用 `player.zza` 输入，其次用水平速度向量点积

- **负面效果抽搐修复**：彻底解决"清除→重新施加→视角闪烁"问题
  + 新增 `MobEffectEvent.Applicable` 事件处理器 — 在效果被添加**之前**直接 `DENY` 拒绝
  + 有害效果（`MobEffectCategory.HARMFUL`）和属性削减效果永远不会进入玩家活跃列表
  + 保留原有 `clearNegativeEffects` 作为兜底清理

- **自动门关门修复**：不再使用 `distSqr` + 缓存的 `BlockState`
  + 改用真实欧几里得距离（≤1.5 格开门，>2.5 格关门，中间缓冲区防抖动）
  + 关门前重新 `getBlockState` 读取最新状态（修复手动关门后无法检测的 bug）
  + 检测频率提升至每 5 tick（原 10 tick），响应更快

- **无限水/岩浆桶重写**：彻底简化 — 只处理手中的空桶，不扫背包
  + 手中空桶 → 删除空桶 → 立即给予满桶（水源优先于熔岩，搜索 3 格）
  + 每 tick 运行，消耗后 0.05 秒内自动补满

### 修复
- **自动补货彻底修复**：
  - 新增 `getCount() <= 0` 双重守卫防止空位填充
  - 不可堆叠物品（`maxStackSize <= 1`）不再触发补货
  - 搜索范围限定主物品栏（索引 9-35），不碰盔甲/副手
  - 物品匹配改为 `getItem() ==` 宽松匹配（忽略 NBT）
  - 只搬运数量更多的堆（`invStack.getCount() > hotbarStack.getCount()`）

## 1.1.6 (2026-07-03)

### 改进
- **水上行走/岩浆行走重写**：从 `teleportTo` 改为 `setPosRaw` + `setPos` 平滑调整位置，不再有画面抖动
  - 潜行时正常下沉穿过流体
  - 已潜入水/岩浆内部时不触发（防止水下被推回表面）
  - 流体表面高度精确计算（`getOwnHeight()`）
  - 参考 MineFargo 的 `FluidCollisionEvent` 设计模式

## 1.1.5 (2026-07-03)

### 修复
- **末影箱便携修复**：恢复 `RightClickBlock` + 空手检测，并跳过有 GUI 的方块（箱子/熔炉等）。现在潜行空手右键普通方块也能打开末影箱，且不会干扰箱子交互。
- **自动补货修复**：不再填补空快捷栏格子，只补充已有但快用完的物品。

### 新增
- **自动关门**：离开门 2 格以上自动关门

## 1.1.4 (2026-07-03)

### 修复
- **末影箱便携修复**：移除 `RightClickBlock` 处理器，改为仅 `RightClickEmpty`（右键空气）触发，并增加空手检测。不再在潜行放置方块时误开末影箱。

## 1.1.3 (2026-07-03)

### 新增 14 个特性
- **自动火把**：光照低于配置值时自动从背包放置火把
- **自动门**：靠近门自动打开
- **洞穴高亮**：海平面以下时周围实体和掉落物自动发光
- **自动钓鱼**：手持鱼竿站在水边自动钓鱼（含宝藏掉落）
- **宝箱磁铁**：自动收集附近箱子中的物品到背包
- **击退免疫**：独立开关控制
- **永久幸运**：永久幸运药水效果
- **低血自动喝药**：生命低于阈值自动使用背包治疗药水
- **受伤闪电反击**：受伤时概率对攻击者召唤闪电
- **击杀连锁**：击杀怪物时伤害周围同种生物
- **冲刺**：双击前进键(W)短距离冲刺
- **自动补货**：快捷栏空了自动从背包补充同种物品
- **无限水/岩浆桶**：空桶自动从附近流体源重新填满

## 1.1.2 (2026-07-03)

### 修复
- **末影箱远程修复**：将 `RightClickEmpty`/`RightClickBlock` 处理器优先级设为 `LOWEST`，确保在神秘遗物（七咒之戒/终界之戒）之后运行，避免事件被截断

## 1.1.1 (2026-07-03)

### 修复
- **飞行修复**：`mayfly` 现在在客户端和服务端同时设置，双跳飞行正常工作
- **虚空救援修复**：阈值从固定 Y=-32 改为基于世界实际最低高度（`getMinBuildHeight() - 4`），主世界仅基岩层以下触发
- **水上行走/岩浆行走修复**：从简单叠加速度改为检测玩家位置+强力推送至表面+归零垂直动量，不再沉入流体
- **描述文本动态化**：物品提示现在读取实时配置值，修改配置后工具提示自动更新

## 1.1.0 (2026-07-03)

### 修复
- 修复 `Player.getRespawnPosition()` 编译错误（1.20.1 需 `ServerPlayer`）
- 修复 `Entity.maxUpStep` 不可见错误（改用方块检测模拟自动上坡）

## 1.0.0 (2026-07-02)

### 初始版本
- 永恒之心 Curios 饰品
- 全套泰拉瑞亚 Fargo's Soul of Eternity 还原功能
- 属性、防御、生命回复、被动效果、机动强化、战斗强化
- 采集强化、生存保障、环境适应、光环系统
- 永怒叠层系统
